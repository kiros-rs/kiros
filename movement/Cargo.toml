[package]
name = "movement"
version = "0.1.0"
authors = ["Angus Finch <developer.finchie@gmail.com>"]
edition = "2018"

[dependencies]
sensor = { path = "../sensor" }
connection = { path = "../connection" }
